/**
 * Centralised contact configuration.
 * All contact links across the site should reference these values
 * so that a single update propagates everywhere.
 */

export const CONTACT_EMAIL = 'work@dev-aditya.com';

/** mailto: link to the primary work address. */
export const CONTACT_EMAIL_HREF = `mailto:${CONTACT_EMAIL}`;

/** Location shown in contact blocks and structured data. */
export const CONTACT_LOCATION = 'Delhi, India';

/** Public GitHub profile — the only verified external profile in the repo. */
export const GITHUB_URL = 'https://github.com/witejackel-eng';
